```javascript
<VStack>
  <Button
    w="100px"
    colorScheme="cyan"
    isLoading={isLoading}
    onClick={handleClick}
    loadingText="Loading"
    spinner={<Spinner color="white" />}
  >
    Click me
  </Button>
</VStack>
```
